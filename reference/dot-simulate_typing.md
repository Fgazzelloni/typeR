# Internal Helper: Type Out Lines

An internal function that simulates typing out a vector of strings.

## Usage

``` r
.simulate_typing(lines, delay = 0.05)
```

## Arguments

- lines:

  A character vector of script lines to simulate typing.

- delay:

  The delay (in seconds) between typing each character (default: 0.05).
